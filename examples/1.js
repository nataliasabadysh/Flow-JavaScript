/* @flow */

/**
 * ↑ Эту инструкцию-комментарий «@flow» называют прагмой.
 * Не забывай указывать прагму «@flow» в начале файла!
 */

/**
 * «number» и «string» называют аннотациями типов.
 */
const a: number = 4;
const b: string = 17;
const c: number = a + b;
